name: Upgrade-26 Wire Format Once

on:
  push:
    branches:
      - upgrade-26-wire-run-lease

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: upgrade-26-wire-run-lease
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install black==26.5.1
      - name: Format lease wiring slice
        run: black src/zcoder/services/continuous_engineering.py tests/unit/test_continuous_engineering.py
      - name: Remove one-shot workflow and commit
        run: |
          rm .github/workflows/upgrade26-wire-format-once.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/zcoder/services/continuous_engineering.py tests/unit/test_continuous_engineering.py .github/workflows/upgrade26-wire-format-once.yml
          if git diff --cached --quiet; then
            exit 0
          fi
          git commit -m "style: format Upgrade-26 pipeline lease wiring"
          git push
